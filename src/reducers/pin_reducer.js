const initialState = {};

const PinReducer = (state = initialState, action) => {
  Object.freeze(state);
  // TODO
  switch (action.type) {
    // case expression:
    //
    //   break;
    default:
      return state;
  }
};

export default PinReducer;
